---
'@jakeginnivan/grove': minor
---

Rename the package to `@jakeginnivan/grove`. The unscoped `grove-cli` name on
npm belongs to an unrelated project, so installs now use
`npm install -g @jakeginnivan/grove`. The `grove` binary and the `wt` shell
function are unchanged.
